var numeros = [1, 2, 3, 4, 5];

var soma = 0;

for(var i =0;i<numeros.length;i++){
   soma+=numeros[i];        //agrega o valor do element(indice para dos burros(obs: vc msm que vai esquercer))                       
}                           //à variável soma até a acabar a quantidade de elements, por isso o loop.

console.log(soma);